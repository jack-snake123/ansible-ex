# vi /etc/systemd/system/docker_autostart.service

[Unit]
 Description=auto start of docker containers
 Requires=docker.service
 After=docker.service

[Service]
 ExecStart=/usr/bin/docker start gitlab jenkins

[Install]
 WantedBy=multi-user.target

# systemctl daemon-reload
# systemctl start docker_autostart.service
# systemctl enable docker_autostart.service
# systemctl status docker_autostart.service
